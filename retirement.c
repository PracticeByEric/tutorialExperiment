#include <stdio.h>
#include <stdlib.h>

//group of basic information needed
typedef struct
{
  int months;
  double contribution;
  double rate_of_return;
}
retire_info;

void retirement(int startAge, double initial, retire_info working, retire_info retiring)
{
  //Compute retirement account balance each month while working

  //Compute retirement account balance each month while retirement
}